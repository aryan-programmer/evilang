fn assert(expr, message){
	if(expr) {
		push_res_stack(true);
	} else {
		if(message == null){
			message = "Expected expression to be true";
		}
		println("Assertion Error: ", message);
		debug(expr);
		push_res_stack(message);
	}
}